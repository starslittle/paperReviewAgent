# DocAgent 命令清单 (以 bylw-pgy 为例)

本清单汇总了运行 DocAgent 的核心命令，以 `bylw-pgy` 文档为例。

## 1. 环境配置

在项目根目录下执行：

```bash
# 安装基础依赖
pip install pdfservices-sdk openpyxl pandas PyMuPDF openai pillow python-dotenv

# 安装 OCR 依赖 (推荐)
pip install paddlepaddle paddleocr
```

> **注意**：请确保根目录下已创建 `.env` 文件并配置了 `ADOBE_CLIENT_ID`、`ADOBE_CLIENT_SECRET` 和 `DEEPSEEK_API_KEY`。

---

## 2. 文档预处理 (Preprocessing)

### 推荐方式：使用统一入口脚本

使用 `run_pipeline.py` 一键运行完整预处理流程（Extract → Process → Build DocIR）：

```bash
# 运行完整流程（包含页面图生成）
python preprocess/run_pipeline.py --doc-id bylw-pgy --with-page-images

# 跳过提取步骤（假设已有 extract_output）
python preprocess/run_pipeline.py --doc-id bylw-pgy --skip-extract --with-page-images

# 只生成页面图
python preprocess/run_pipeline.py --doc-id bylw-pgy --only-page-images
```

### 传统方式：分步执行

如需精细控制各步骤，可分别执行以下三个主流程步骤：

#### Step 1: Extract（提取文档内容）

使用 MinerU API 提取文档结构（支持 PDF/DOC/DOCX）。

```bash
python preprocess/step1_extract.py --raw-data-dir ./data --result-dir preprocess/extract_output --doc-id bylw-pgy
```

#### Step 2: Process（清洗与标准化数据）

处理提取出的数据，标准化为内部格式。

```bash
python preprocess/step2_process.py --extract-data-dir preprocess/extract_output/MinerU --save-dir preprocess/processed_output/MinerU --doc-id bylw-pgy
```

#### Step 3: Build DocIR（构建 XML 树）

构建层级化的 DocIR 与 XML 树，用于 Agent 检索。

```bash
python preprocess/step3_build_docir.py --processed-dir preprocess/processed_output/MinerU --doc-id bylw-pgy --output-dir sample_results
```

#### Tool: Make Page Images（可选，生成页面图）

为 Vision Agent 准备多模态图像数据（独立步骤，不依赖主流程）。

```bash
python preprocess/tool_make_page_images.py --raw-data-dir ./data/bylw-pgy --save-dir preprocess/processed_output/MinerU --resolution 144
```

---

## 3. 运行 DocAgent 审查 (Review)

预处理完成后，即可运行 Agent 进行文档智能审查。

### 方式 1：运行单文档审查 (推荐)

```bash
# 1. 执行审查 (生成 review_bylw-pgy.json)
python review_runner.py --doc-id bylw-pgy

# 2. 生成可视化 HTML 报告
python generate_report.py --doc-id bylw-pgy
```

### 方式 2：使用一键化脚本 (Windows PowerShell)

```powershell
# 运行完整审查并自动生成报告
.\scripts\run_pipeline.ps1 -DocName "bylw-pgy"
```

### 方式 3：运行简化版审查脚本

```bash
# 该脚本会自动运行审查并生成 HTML 报告
python run_review.py
```

---

## 4. 查看结果

审查完成后，结果保存在 `sample_results/` 目录下：

- **审查报告 (HTML)**: `sample_results/report_bylw-pgy.html` (可视化查看)
- **审查细节 (JSON)**: `sample_results/review_bylw-pgy.json` (包含详细问题及页码)
- **文档大纲 (XML)**: `sample_results/outline_bylw-pgy.xml`
- **文档全树 (XML)**: `sample_results/tree_bylw-pgy.xml`
